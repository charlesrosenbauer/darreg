#include "render.h"
#include "layout.h"



void drawPanel(Panel p, Window window){
	if(!p.edited) return;
	
	int h, w, x, y;
	// TODO: calculate panel size and offset
	
	switch(p.type){
		default: break;
	}
}
